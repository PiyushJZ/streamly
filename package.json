{
  "name": "streamly",
  "private": true,
  "devDependencies": {
    "husky": "^9.1.7",
    "prettier": "^3.5.3"
  },
  "scripts": {
    "prepare": "husky",
    "lint": "pnpm -r --parallel lint",
    "build": "pnpm -r --stream build"
  }
}