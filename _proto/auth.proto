syntax = "proto3";

package auth;

service AuthService {
  rpc GetUsers(Empty) returns (Users);
}

message Empty {}

message Users {
  repeated User users = 1;
}

message User {
  string id = 1;
  string name = 2;
  string email = 3;
}